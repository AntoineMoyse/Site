@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Vérification code de récupération";
}

<h1 style="margin-top:110px;">@ViewData["Title"]</h1>
<hr />
<p>
    Vous avez demandé de vous connecter avec un code de récupération. Cette connexion ne sera pas mémorisée tant que
    vous n'aurez pas fourni un code de l'application de double authentification au moment de la connexion 
    ou que vous désactivez la double authentification et vous reconnectez.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Se Connecter</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
